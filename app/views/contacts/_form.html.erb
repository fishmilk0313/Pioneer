<%= form_with(model: contact, local: true) do |form| %>
  <% if contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :"氏名" %><br>
    <%= form.text_field :name ,class:"form-control",size: 50%>
  </div>

  <div class="field">
    <%= form.label :email %><br>
    <%= form.text_field :email, class:"form-control", size: 50 %>
  </div>

  <div class="field">
    <%= form.label :"お問い合わせ内容" %><br>
    <%= form.text_area :content,class:"form-control", size: 50  %>
  </div>

  <div class="actions">
    <%= form.submit "送信する", class:"btn btn-primary btn-lg btn-block" %>
  </div>
<% end %>
